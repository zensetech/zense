rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    match /patientSubmissions/{document=**} {
      allow create: if true;
      allow read, update, delete: if false; // Deny read, update, and delete for now
    }
  }
}
